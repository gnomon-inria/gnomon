package:
  name: gnomon_plugin_{{package_name}}
{% raw %}
  version: {{ environ.get('GIT_DESCRIBE_TAG', 'default') }}

source:
  path: ../

build:
  number: {{ GIT_DESCRIBE_NUMBER }}
  preserve_egg_dir: True
{% endraw %}

requirements:
  build:
    - setuptools
    - python=${PYTHON_VERSION}.*
    - gnomon=${gnomon_VERSION}
  host:
    - python=${PYTHON_VERSION}.*
    - gnomon=${gnomon_VERSION}
  run: 
    - python=${PYTHON_VERSION}.*
    - gnomon=${gnomon_VERSION}
    - ipython
    - nose2
    - coverage

about:
  home: {{url}}
  license: {{license}}
  summary: {{short_descr}}
