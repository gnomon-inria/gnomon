name: plugin_{{package_name}}
channels:
  - dtk-forge6
  - gnomon
  - mosaic
  - morpheme
  - defaults
  - conda-forge
dependencies:
  - python=${PYTHON_VERSION}.*
  - ipython
  - nose2
  - coverage
  - gnomon=${gnomon_VERSION}
  