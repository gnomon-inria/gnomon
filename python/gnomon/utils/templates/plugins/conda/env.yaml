name: plugin_{{package_name}}
channels:
  - dtk-forge
  - gnomon
  - defaults
  - conda-forge
dependencies:
  - python=${PYTHON_VERSION}.*
  - ipython
  - nose
  - coverage
  - gnomon=${gnomon_VERSION}
  