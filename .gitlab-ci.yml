image: conda/miniconda3

pages:
  stage: deploy
  script:
  - cd doc
  - conda update --all
  - conda create -n gnomon
  - conda activate gnomon
  - conda install sphinx make
  - make html
  - mv build/html ../public
  artifacts:
    paths:
    - public
  only:
  - master
  - develop
