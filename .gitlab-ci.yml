image: conda/miniconda3

pages:
  script:
  - cd doc
  - conda update --all
  - conda install -c sphinx make
  - make html
  - mv build/html ../public
  artifacts:
    paths:
    - public
  only:
  - master
  - develop
